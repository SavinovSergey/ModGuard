# Тестирование (pytest).
# Подключайте вместе с нужным набором зависимостей, например:
#   pip install -r requirements/core.txt -r requirements/test.txt

pytest>=7.4.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
